---
description: Install Alex Tong's Claude Code status line into ~/.claude/
---

Run exactly this command and print its stdout verbatim:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/install.sh --statusline
```

Don't add commentary, explanation, or next-step suggestions — the script's output already says what happened and what the user should do. If the command exits non-zero, show the error verbatim and stop.
